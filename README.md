# Emergency Guardian

Emergency Guardian 是一个基于 Web3 智能合约、零知识证明(ZKP)和 AI 代理的去中心化紧急数据管理系统。

## 🎯 核心功能

- **去中心化治理**: 基于智能合约的自动化治理，无需中心化权威
- **零知识隐私**: 使用 ZKP 技术在验证身份和权限的同时保护用户隐私
- **AI 辅助决策**: 智能代理分析紧急情况并提供可验证的决策支持
- **多层级安全**: 结合时间锁、多签验证和渐进式权限升级
- **可验证透明**: 所有操作在区块链上可审计，同时保护敏感信息

## 🏗️ 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   Frontend      │    │   Backend       │    │   AI Agents     │
│   (React+Vite)  │◄──►│  (TypeScript)   │◄──►│   (Python)      │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         ▼                       ▼                       ▼
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│  Smart Contract │    │ Storage Service │    │   ZK Circuits   │
│   (Solidity)    │    │      (Go)       │    │  (Circom+JS)    │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

## 🚀 快速开始

### 环境要求

- Node.js >= 18.0.0
- Python >= 3.9
- Go >= 1.21 (可选)
- Foundry (Solidity 开发)

### 安装依赖

```bash
# 安装Node.js依赖
npm install

# 安装Python依赖
cd ai-agents
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt

# 安装Foundry (如果未安装)
curl -L https://foundry.paradigm.xyz | bash
foundryup
```

### 环境配置

```bash
# 复制环境变量模板
cp .env.example .env

# 编辑.env文件，填入你的配置
# - 私钥和RPC端点
# - API密钥等
```

### 启动开发环境

```bash
# 启动所有服务 (使用Docker Compose)
docker-compose up -d

# 或者分别启动各个服务
npm run dev:frontend    # 前端 (端口3000)
npm run dev:backend     # 后端 (端口3001)
npm run dev:ai-agents   # AI代理 (端口8001)
npm run dev:storage     # 存储服务 (端口8002)
```

### 部署智能合约

```bash
cd contracts

# 编译合约
forge build

# 运行测试
forge test

# 部署到测试网 (需要配置.env)
forge script script/Deploy.s.sol --rpc-url $RPC_URL_GOERLI --broadcast
```

## 📁 项目结构

```
emergency-guardian/
├── contracts/           # Solidity智能合约 (Foundry)
├── frontend/           # React前端应用
├── backend/            # TypeScript后端API
├── ai-agents/          # Python AI代理服务
├── storage-service/    # Go存储服务
├── zk-circuits/        # 零知识证明电路
├── docs/              # 项目文档
└── docker-compose.yml # Docker编排配置
```

## 🔧 开发指南

### 智能合约开发

基于 OmniGuardianV3 合约，主要增强：

- ZK 证明验证集成
- 多签阈值验证强化
- IPFS 数据存储集成

### 测试

```bash
# 运行所有测试
npm run test

# 智能合约测试
npm run contracts:test

# 前端测试
npm run test --workspace=frontend

# 后端测试
npm run test --workspace=backend
```

## 📚 文档

- [架构设计](docs/architecture.md)
- [API 文档](docs/api/)
- [部署指南](docs/deployment/)

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License
