{
  "name": "emergency-guardian-zk-circuits",
  "version": "1.0.0",
  "description": "Zero-Knowledge Proof circuits for Emergency Guardian system",
  "main": "js/proof-generator.js",
  "scripts": {
    "compile": "circom circuits/identity.circom --r1cs --wasm --sym -o build/ && circom circuits/emergency.circom --r1cs --wasm --sym -o build/ && circom circuits/authorization.circom --r1cs --wasm --sym -o build/",
    "setup": "node scripts/setup.js",
    "test": "mocha test/*.test.js --timeout 60000",
    "test:jest": "jest",
    "generate-keys": "node scripts/generate-keys.js",
    "clean": "rm -rf build/* keys/*"
  },
  "keywords": [
    "zero-knowledge",
    "circom",
    "snarkjs",
    "emergency-guardian",
    "blockchain",
    "privacy"
  ],
  "author": "Emergency Guardian Team",
  "license": "MIT",
  "dependencies": {
    "snarkjs": "^0.7.3",
    "circomlib": "^2.0.5",
    "ffjavascript": "^0.2.60"
  },
  "devDependencies": {
    "circom_tester": "^0.0.19",
    "mocha": "^10.2.0",
    "chai": "^4.3.10",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
