# 🌐 Emergency Guardian 公网部署指南

## 🚀 快速部署 (推荐方案)

### 方案 1: Vercel 部署 (最简单)

1. **安装 Vercel CLI**

   ```bash
   npm install -g vercel
   ```

2. **构建前端**

   ```bash
   cd frontend
   npm run build
   cd ..
   ```

3. **部署到 Vercel**

   ```bash
   vercel --prod
   ```

4. **访问你的网站**
   - Vercel 会提供一个公网 URL，如: `https://emergency-guardian-xxx.vercel.app`

### 方案 2: Railway 部署 (支持后端)

1. **安装 Railway CLI**

   ```bash
   npm install -g @railway/cli
   ```

2. **登录并部署**

   ```bash
   railway login
   railway init
   railway up
   ```

3. **访问你的网站**
   - Railway 会提供一个公网 URL，如: `https://emergency-guardian-xxx.railway.app`

### 方案 3: Netlify 部署 (免费静态托管)

1. **安装 Netlify CLI**

   ```bash
   npm install -g netlify-cli
   ```

2. **构建并部署**

   ```bash
   cd frontend
   npm run build
   cd ..
   netlify deploy --prod --dir=frontend/dist
   ```

3. **访问你的网站**
   - Netlify 会提供一个公网 URL，如: `https://emergency-guardian-xxx.netlify.app`

## 🔧 一键部署脚本

运行自动化部署脚本：

```bash
./deploy-to-public.sh
```

## 📋 部署前检查清单

- ✅ 前端应用构建成功 (`npm run build`)
- ✅ 所有环境变量配置正确
- ✅ 智能合约地址正确
- ✅ 区块链网络配置正确 (Sepolia)

## 🌍 部署后验证

部署完成后，访问你的公网 URL 并检查：

1. **基本功能**

   - ✅ 网站正常加载
   - ✅ 页面导航正常
   - ✅ 响应式设计正常

2. **Web3 功能**

   - ✅ MetaMask 连接正常
   - ✅ 网络切换到 Sepolia
   - ✅ 合约交互正常

3. **核心功能**
   - ✅ 紧急求助页面正常
   - ✅ 监护人管理正常
   - ✅ 设置页面正常

## 🔗 重要配置

### 环境变量

```env
VITE_BLOCKCHAIN_RPC_URL=https://sepolia.infura.io/v3/9aa3d95b3bc440fa88ea12eaa4456161
VITE_CONTRACT_ADDRESS=0x6af445EA589D8f550a3D1dacf34745071a4D5b4F
VITE_CHAIN_ID=11155111
VITE_NETWORK_NAME=sepolia
```

### 智能合约信息

- **网络**: Sepolia Testnet
- **合约地址**: `0x6af445EA589D8f550a3D1dacf34745071a4D5b4F`
- **ZK 验证器**: `0xf9D10528B5b1837cd12be6A449475a1288832263`

## 🎯 推荐部署流程

1. **选择平台**: Vercel (最简单) 或 Railway (功能最全)
2. **运行部署脚本**: `./deploy-to-public.sh`
3. **验证功能**: 访问公网 URL 测试所有功能
4. **分享链接**: 将公网 URL 分享给用户

## 🆘 常见问题

### Q: 部署后网站无法访问？

A: 检查构建是否成功，确认所有文件都已上传

### Q: MetaMask 连接失败？

A: 确认环境变量中的 RPC URL 和合约地址正确

### Q: 页面导航不工作？

A: 确认 SPA 路由重定向配置正确 (已在配置文件中设置)

### Q: AI 服务无法访问？

A: 需要单独部署 AI 服务，或使用 Railway 全栈部署

## 🎉 部署成功！

部署完成后，你将获得一个公网可访问的 Emergency Guardian 系统！

**示例 URL**: `https://emergency-guardian-demo.vercel.app`

用户可以通过这个 URL 访问完整的紧急守护功能，包括：

- 钱包连接
- 紧急求助
- 监护人管理
- 智能合约交互
